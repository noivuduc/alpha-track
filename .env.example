# Root .env — Docker Compose credentials only.
# Copy to .env and change passwords before running in production.

POSTGRES_USER=alphatrack
POSTGRES_PASSWORD=changeme
POSTGRES_DB=alphatrack
REDIS_PASSWORD=changeme
